1. Необходимо в отдельных пакетах реализовать АТД Множество (упорядоченное) на двоичных векторах, на связном списке, на кольцевом связном. Необходимые операции (открытые методы) перечислены ниже.
2. Реализовать класс с методом main(), в котором тестируются реализованные операции для АТД Множество. Метод main() должен правильно работать со всеми представлениями. При этом в классе можно менять только импортируемый пакет.

АТД Множество
A, B, C – множества, x – элемент.

Операции (указаны все и явные, и неявные параметры):
Операции UNION(A, В, С), INTERSECTION(A, В, С) и DIFFERENCE(A, В, С) имеют «входными» аргументами множества A и В, а в качестве результата — «выходное» множество С, равное соответственно А U В, А ∩ В и А \ В.
MERGE(A, В, С) – присваивает множеству С значение А U В, но результат будет не определен, если А ∩ В ≠ 0, т. е. в случае, когда множества А и В имеют общие элементы. MERGE не отличается от оператора объединения двух множеств, но предполагается, что множества-операнды не пересекаются (т. е. не имеют общих элементов).
MEMBER(А, x) – имеет аргументами множество А и объект х того же типа, что и элементы множества А, и возвращает булево значение true (истина), если х принадлежит А, и значение false (ложь), если х не принадлежит A.
MAKENULL(A) – присваивает множеству А значение пустого множества.
INSERT(А, x) – делает х элементом множества А. Другими словами, новым значением множества А будет A U {х}. В случае, когда элемент х уже присутствует в множестве А, это множество не изменяется в результате выполнения данной операции.
DELETE(А, x) – удаляет элемент х из множества А, т. е. заменяет множество А множеством А \ {х}. Если элемента х нет в множестве А, то это множество не изменяется.
ASSIGN(A, В) – присваивает множеству А в качестве значения множество В.
MIN(А) – возвращает наименьший элемент множества А. Для применения этой операции необходимо, чтобы множество А было параметризировано и его элементы были линейно упорядочены. Например, MIN({2, 3, 1}) = 1 и MIN({'а', 'b', 'с'}) — 'а'. Подобным образом определяется операция МАХ.
EQUAL(A, В) – возвращает значение true тогда и только тогда, когда множества А и В состоят из одних и тех же элементов.
FIND(x) – оперирует в среде, где есть набор непересекающихся множеств. Операция возвращает имя (единственное) множества, в котором есть элемент х.

Лабораторная работа состоит из двух этапов: проектирование и реализация. Пока не принят этап проектирования, реализация не рассматривается!
Проект представляется в виде текстового файла, в котором описываются все классы и их данные. Кроме того, на естественном языке перечисляются все действия для всех конструкторов, операций и вспомогательных методов.

Общие требования:
Не использовать коллекции.
Не использовать доступ по умолчанию. Указывать доступ для всего, классов, данных, методов.
Для всех классов перед каждым методом (кроме main()), в комментариях должно быть записано: какую задачу решает метод, какие параметры ему передаются, что возвращается в результате.
Для всех объявленных в методах переменных, включая метод main(), в комментариях необходимо указать их назначение.
Все важные для понимания методов моменты должны сопровождаться комментариями.
